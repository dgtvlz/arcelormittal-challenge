
apt install -y protobuf-compiler
    protoc --version
    libprotoc 3.12.4

python3 -m pip install grpcio
python3 -m pip install grpcio-tools

python3 -m grpc_tools.protoc -I../proto \
    --python_out=../src/proto_files \
    --pyi_out=../src/proto_files \
    --grpc_python_out=../src/proto_files \
    ../proto/sales.proto


docker build -t dgtvlz/arcelor-grpc-client -f ClientDockerfile .
docker build -t dgtvlz/arcelor-grpc-server -f ServerDockerfile .

docker network create grpc-network

docker run -d --name grpc-server --network grpc-network -p 50051:50051 -p 50052:50052 -v db:/app/db dgtvlz/arcelor-grpc-server

docker run --network grpc-network dgtvlz/arcelor-grpc-client /app/data/2023/7/28/05694.json


